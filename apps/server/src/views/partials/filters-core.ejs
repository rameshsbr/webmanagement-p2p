<!-- Shared filter grid wrapper.
     This keeps the same grid semantics everywhere (minmax(0,1fr) columns). -->
<div class="filters-grid">
  <!-- (intentionally empty: pages include filters-bar.ejs which renders the actual controls) -->
</div>